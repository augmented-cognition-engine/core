# ACE 1.2.0 public acceptance run — RUN_LOG

Run start (UTC): 2026-08-20T13:42:37Z
Operator: automated first-time-user session (Claude agent), per RUN_BRIEF.md (frozen)
Workspace: ~/ace-12-acceptance/ (only path used; no repository clone; no other checkout touched)

## Host facts (verified 13:42:37Z)

| Fact | Value |
|---|---|
| macOS | 26.6 (build 25G72) |
| Python | 3.12.13 |
| uv | 0.11.14 |
| Docker | Engine 29.6.2, Compose v5.3.1 |
| claude CLI | 2.1.224 (signed in) |

---

## Step log

### S0 — Workspace prep
- 13:42Z: Verified `~/ace-12-acceptance/` exists and contains only RUN_BRIEF.md. Host facts recorded above.

### S1 — J1 Install: locate public artifacts (13:42–13:44Z)
- 13:42Z: Fetched release page `v1.2.0` ("ACE 1.2.0 — Personal Intelligence"). Release notes state J1–J10 public acceptance still ahead; known limitations disclosed (no runnable restore; deletion not universal erasure; single-node).
- 13:43Z: Saved release asset list via public GitHub API to `public-docs/release-v1.2.0.json` (16 assets: 8 wheels + 8 sdists). Saved PyPI metadata for `ace-core==1.2.0` (`public-docs/ace-core-1.2.0-pypi.json`) and the package README (`public-docs/ace-core-readme.md`).
- PyPI dists: `ace_core-1.2.0-py3-none-any.whl` sha256 `6e7d11c658f01f4524fe6b1720f27a74765be6c53074f48c9a90b36e63f373c7`; sdist `65dcece805a33d67414a2c9135519410bfca3f16a9a26ccf3da520ac01ec6c67`.
- 13:43Z: Downloaded the 8 attached wheels into `artifacts/`; local `shasum -a 256` matched the release-declared digest for ALL 8 (csv d191cc05…, json ccf615a1…, markdown 0d36aa69…, pdf 0810e2ed…, normalizers c00b0b39…, bundle cac7ad60…, pack ad97a719…, workspace-action 600871f7…). PASS: artifact integrity.
- **Deviation D1 (docs):** `docs/getting-started.md` at tag v1.2.0 still titles itself the "supported ACE 1.0.3 installation" and pins `pip install ace-core==1.0.3`; the full self-hosted runtime path requires `git clone` (disqualified by this brief). No documented package-only runtime launch path exists.
- **Deviation D2 (docs):** README on PyPI says "The current published stable package and public-index install is `ace-core==1.2.0`, the bounded **ACE 1.1 Code Intelligence** release" — version/name mismatch inside one sentence.

### S2 — J1 Install: install + first run (13:44–13:47Z)
- 13:44Z: `uv venv --python 3.12 .venv`; `uv pip install ace-core==1.2.0 <8 local wheels>` → success. `ace.__version__` = **1.2.0**; adapters all 0.1.0, workspace-action 0.5.0, bundle+pack 0.1.0, surrealdb client 2.0.0.
- **Failure F1 (first-run):** `ace --help` (and `ace setup`) crash with a raw pydantic ValidationError traceback — `Settings.jwt_secret` required, no `.env` exists. The bootstrap command that is supposed to generate `.env` cannot itself start without one. First-time-user experience on the package-only path is a stack trace.
- **Workaround W1 (invented):** found `core/engine/runtime/local.env.example` shipped inside the wheel; copied it to `./.env`, replaced JWT_SECRET with `secrets.token_hex(32)`, replaced API_KEY with a random hex, set `REQUIRE_SUBSCRIPTION=1`, added `FORCE_CLI_PROVIDER=1` (host has signed-in `claude` CLI = documented provider slot 9 in docs/providers.md). Not documented anywhere public; discovered by inspecting installed package contents.
- 13:47Z: `ace --help` now prints full command tree (setup, doctor, service, atrium, ownership, briefing, …). `ace setup --help` shows `--provider claude-cli`, `--non-interactive`, `--skip-first-task`, and `--project-dir` ("Path to the ace-core checkout" — checkout-shaped default again).

### S3 — J1 Install: first `ace setup` attempt → host-collision incident (13:46–13:52Z)
- 13:46:46Z: `ace setup --provider claude-cli --non-interactive --skip-first-task` (cwd = workspace).
- **Failure F2 / Incident I1 (isolation):** this host already runs a developer ACE stack (SurrealDB `infra-surrealdb-1` on 127.0.0.1:8001, an ACE API answering on 127.0.0.1:3000, config dir `~/.ace`). `ace setup` has NO isolation defaults: it connected to the existing SurrealDB at `ws://localhost:8001`, found schema v177 already applied, and **applied migrations v178 and v179 to that pre-existing database**; it then declared "ACE API is already running" because the foreign API answered `/health/live` on hardcoded port 3000, and failed with `401 Unauthorized` at automatic login (foreign API, different API key). Setup exited with an error after mutating out-of-workspace state.
- Unintended out-of-workspace writes by `ace setup` (defaults, not my file operations), disclosed for reconciliation: `~/.ace/runtime/.env`, `~/.ace/runtime/.env.example`, `~/.ace/runtime/infra/docker-compose.yml` (created 06:46:47 local), one append to `~/.ace/onboarding.jsonl` (06:46:50), and schema migrations v178+v179 applied to the dev SurrealDB. `~/.ace/token.json` untouched. Left in place rather than risking further out-of-workspace modification.
- **Finding:** the API port is hardcoded to 3000 (`_API_URL` in `cli/commands/setup.py`; uvicorn started with `--port 3000`); the setup error path even says "set up ACE on a free port" but no flag or env var exists for it. SurrealDB port/volume ARE isolatable (`ACE_SURREAL_HOST_PORT`, `COMPOSE_PROJECT_NAME` — documented only as comments in `local.env.example`). Config dir isolatable only via undocumented `ACE_CONFIG_DIR`.
- **Workaround W2 (invented):** full manual isolation — `ACE_CONFIG_DIR=~/ace-12-acceptance/.ace`, `COMPOSE_PROJECT_NAME=ace12accept`, `ACE_SURREAL_HOST_PORT=18041`, `SURREAL_URL=ws://localhost:18041`, own SurrealDB container from the shipped `local-compose.yml`, own API via the documented manual uvicorn path on port 13000, CLI pointed at it with `ACE_URL`.

### S4 — J1 Install: isolated runtime launch (13:49–13:52Z) — J1 RESULT: PASS with deviations
- 13:49Z: `.env` updated: `SURREAL_URL=ws://localhost:18041`, `COMPOSE_PROJECT_NAME=ace12accept`, `ACE_SURREAL_HOST_PORT=18041`. Started dedicated SurrealDB (`surrealdb/surrealdb:v3.2.3`, container `ace12accept-surrealdb-1`, own volume) from the shipped `local-compose.yml` → healthy.
- 13:50:08Z: `python -m scripts.schema_apply` (scripts package IS shipped in the wheel) → "applied 178 file(s); schema v179 validated" on the fresh DB.
- 13:50Z: started API manually: `python -m uvicorn core.engine.api.main:app --host 127.0.0.1 --port 13000` with `ACE_CONFIG_DIR=~/ace-12-acceptance/.ace`; `/health/live` → `{"status":"ok"}`; first-run seeded 177 instrument frameworks. Created `acecli.sh` wrapper (pins ACE_CONFIG_DIR + ACE_URL) for every subsequent CLI call.
- 13:51Z: `ace login --api-key <from .env>` → token saved to workspace `.ace/token.json` (0600). `ace doctor`: PASS configuration, surrealdb, schema 179/179, model_policy (valid), api, authentication, mcp 11/11 — but "ACE is not operationally ready" while provider is `configured_unverified` (honest no-token-spent default; doctor points at `ace setup` for repair, which is the command that fails on this host).
- 13:52:09–17Z: `ace doctor --live-provider` → `PASS model_provider` (one minimal diagnostic call), resolver slot 9, `selected_by: forced_cli_provider`, billing `cli_provider` subscription. Runtime operationally ready.
- Versions as reported: `ace.__version__` 1.2.0; schema v179; SurrealDB server v3.2.3 (pinned in shipped compose); models fast=claude-haiku-4-5-20251001, capable=claude-sonnet-5, reasoning=claude-opus-4-8, frontier=claude-fable-5.
- J1 verdict: **PASS (with deviations D1, F1/W1, F2/I1/W2)** — installation from public artifacts succeeded and the runtime reached verified readiness, but never through the documented path.

### S5 — J2 Choose (13:53–14:12Z) — J2 RESULT: PASS with major deviation
- 13:55Z: `GET /v1/intelligence/catalog/packs` → 200; **Personal Intelligence** pack listed (pack_id `personal_intelligence` 1.0.0, distribution `ace-personal-intelligence-pack` 0.1.0, manifest digest sha256:375fc5c5…), with digest-pinned ontology + source-mapping modules. Pack README says installation makes it "discoverable in the catalog so a user can choose it (J2)" — true.
- **Observation O1:** the catalog lists ONLY Personal Intelligence. "Beside the existing catalog entries" does not hold literally: Code Intelligence appears nowhere in `catalog/packs` (it is core-integrated, surfaced via `/v1/code-intelligence/*` endpoints and a dedicated Atrium module), and `catalog/profiles` returned **empty**.
- **Failure F3 (release gap):** the entire Choose→Connect→First-Brief Builder flow (the only flow Atrium's UI wires for this journey — verified by inspecting the packaged SPA's endpoint strings) requires an installed onboarding profile (`domain_packs/**/onboarding_profile.json` in an installed distribution). **No public v1.2.0 artifact ships one.** `POST /v1/intelligence/builds/prepare` → `404 {"detail": "Intelligence onboarding profile is not installed"}`. A first-time public user cannot start the Personal Intelligence journey from published artifacts. (Also noted: prepare with a future `requested_at` is refused with 409 "requested_at cannot be materially in the future" — honest clock guard.)
- **Workaround W3 (invented, material):** authored a local onboarding profile conforming to `ace.intelligence.onboarding-profile/v1alpha1` (validated by the installed contract model; digest computed by the contract itself) and hand-assembled a wheel `ace12_local_onboarding_profile-0.0.1-py3-none-any.whl` (sha256 c6d432a6021309c31b82019f9bebd77631c372a928b7def0f603a55c98760717, in `artifacts-local/`) shipping it at `domain_packs/personal_intelligence/onboarding_profile.json`. After install, `catalog/profiles` lists profile `personal_intelligence` digest sha256:a7460fd4… . THIS IS A NON-PUBLIC ARTIFACT: every downstream step depends on it, which taints strict "public artifacts only" acceptance for J2–J10.
- **Observation O2:** the pack's source-mapping module maps ONLY `local_markdown_notes` (markdown.note → note entity). The PDF/CSV/JSON adapters ship as pure parsers, but no pack mapping references them.
- 14:12Z: `ace atrium --port 15173 --no-open` serves the packaged SPA at `/atrium` (HTTP 200) with same-origin API forwarding; left running for the session. UI interaction is exercised at the API layer this UI calls (the exact endpoint set was extracted from the shipped JS).

### S6 — J3 Connect (14:00–14:20Z) — J3 RESULT: BLOCKED (release gap), corpus authored
- 14:00Z: Authored the personal corpus inside the workspace at `corpus/`: Obsidian-style folder `corpus/notes/` (garden_journal.md cbec0c39…, watering_rules.md 4a82d598…, reading_list.md 10f65e6a…, with `[[wikilinks]]` and frontmatter), `corpus/soil_test_2026.pdf` (hand-built one-page PDF, a26589da…), `corpus/harvest_2026.csv` (62cf2690…), `corpus/subscriptions.json` (2aced540…). Full sha256 digests recorded via `shasum -a 256`.
- **Failure F4 (release gap, blocking):** `POST /v1/intelligence/builds/prepare` with the (workaround-installed) profile → **503 `"no Intelligence build planner is registered for profile: personal_intelligence"`**. The planner registry loads entry points from group `ace.intelligence_build_planners`; the installed public artifact set registers **zero** planners (verified via `importlib.metadata.entry_points`), and no distribution ships an implementation of `IntelligenceBuildPlannerV1Alpha3` (only the Protocol, registry, and plan-validation code). The component that would produce the reviewable Connect plan — exact folder scope, access mode, expected contribution shown BEFORE any read — does not exist in the public release.
- Consequence: sources can never be authorized, so the consent-before-read property (the heart of J3) is **untestable** from public artifacts. Writing my own planner would test my code, not ACE; per the brief this is recorded as a finding instead.
- 14:04Z: `POST /auth/local-owner/bootstrap` → 200; five fixed local-owner grants created (atrium-intelligence-build, atrium-observe-read, atrium-resource-feedback, personal-export, personal-delete). Grants do not unblock F4 (planner registry is empty regardless).
- Cross-check against the maintainer's RC disposition (`docs/evidence/ace-1.2.0-local-release-candidate-v1.md`): the local gate verified release-guard tests, lint, reproducible builds, and bundle discovery — it never executed the user journey from installed artifacts, which is exactly where F3/F4 live.
- Also confirmed absent: any HTTP or CLI surface for source inventory (J4), and any caller of the shipped `ace.application.solution_bundle_activation` library (bundle activation is reachable only through the undocumented Python API; `GET .../packs/activations/personal_intelligence` → 404 "no activation exists").

### S7 — J4 Inventory — RESULT: BLOCKED
- No inventory/permission/readiness/processing/freshness/health surface exists for personal sources in the public release (no HTTP route, no CLI command; Atrium's builder projection depends on the blocked build flow). `ace briefing` reports the honest empty state ("No briefings yet"). `ace onboarding report` exists for first-run evidence only.

### S8 — J5 First Brief — RESULT: BLOCKED (upstream F3/F4)
- With no way to connect sources, no Brief over the corpus can exist. `GET /briefings/latest` equivalent CLI reports none. Not attempted further; would require inventing the entire ingestion path.

### S9 — J7 Ask / J8 Correct (14:04–14:06Z) — RESULTS: J7 PARTIAL PASS, J8 PARTIAL (fail-closed verified; happy path blocked)
- J7 (honest no-answer): `POST /v1/intelligence/ask` answers with contract `ace.intelligence.ask-no-answer/v1alpha1` for both a corpus-shaped question ("What tomato varieties did I plant this spring?") and an out-of-corpus question ("What is the population of Lisbon?"), each naming exactly what is missing (`missing_coverage:no_authorized_brief_resources_available`) and carrying a complete authority-use receipt (grant head, receipt ids, digests). The honest-refusal contract works; **cited answers could not be tested** because no Brief can exist (F3/F4).
- J8 (claim binding): `POST /v1/intelligence/ask/corrections` is strictly claim-bound and fail-closed. Underspecified targets → 422 with exact field errors; correction_intent is a closed enum (`outdated | entity_mapping_wrong | missing_source | source_overweighted`); a fully-formed correction against a nonexistent Brief revision → **409 "target Brief exact revision is unavailable"**. No silent recording. The governed re-derivation proposal on a REAL claim could not be tested (blocked upstream).
- 14:04Z: `POST /auth/local-owner/bootstrap` (documented nowhere public; found via OpenAPI) is the source of the fixed owner grants used above.

### S10 — J9 Restart (14:06–14:07Z) — RESULT: PASS (scoped to what exists)
- 14:06:27Z stopped the API (pid 21219) and SurrealDB container; `/health/live` refused. 14:06:59Z restarted container (healthy) + API; `/health/live` ok. Total ~32 s.
- Identity checks after restart: local-owner bootstrap reports all five grants **"verified"** (not re-created); pack manifest digest sha256:375fc5c5… identical; profile digest sha256:a7460fd4… identical; the observe-read grant's state head identical (sequence 1, `authority_grant_revision:f4241df7…`, `governed_state_commit:d971bdf5…`); pre-restart immutable record `task_authentication_receipt:7d5bf322…` still present in export (record_count 5); pre-restart CLI token still authenticates. Sources/Briefs/revisions/corrections could not be included in the check (they never existed — blocked upstream).
- **Observation O3:** `/health/system` reports the background worker probe down (connection refused) under this manual package-only launch; `ace doctor` never surfaced a worker requirement. Unclear from public docs whether a separate worker process is part of the supported runtime.

### S11 — J10 Own (14:07–14:08Z) — RESULT: PASS on exercised surfaces (scope-limited)
- Export: `POST /v1/intelligence/ownership/export` → canonical immutable records, each with `material_hash`; set-level `record_set_digest`; artifact id + `artifact_digest`; `portability_scope: "canonical-records-only; runnable restore is not provided"`, `runnable_restore_supported: false` — matches the release's carried limitation truthfully.
- Deletion preview: exact `record_count: 5`, per-record listing, 6 derived-artifact entries, `record_set_digest`, a `confirmation_digest`, a 10-minute expiry, `backup_non_reappearance_proven: false` with explicit backup limitation, and a surviving-derivative disclosure naming embeddings, graph rows, indexes, caches, backups, prior exports, external copies. Exclusions are disclosed, not hidden.
- Confirm (deliberately not HTTP DELETE): accepted only with the reviewed preview + digest → deletion proof: `removed_count: 5`, `primary_store_non_reappearance_verified: true`, per-store derived-artifact erasure entries each with enumerated/removed/surviving counts and `verified_absent: true`.
- Independent non-reappearance check: re-export → `record_count: 0`, zero overlap with the 5 deleted record keys; fresh preview refuses with 409 (message "Personal Intelligence ownership request is stale or conflicted" — fail-closed but unhelpfully worded for the empty case; minor UX finding).
- Scope limit: only authority/authentication receipts existed to delete; deletion of Brief/source-derived intelligence could not be exercised (blocked upstream).

### S12 — Wind-down (14:09Z–)
- Reported versions: API surface version "v1 stable"; `ace.__version__` 1.2.0; schema v179; SurrealDB v3.2.3 (pinned image); package set as recorded in S2.
- Runtime left in workspace: `.venv`, `corpus/`, `runtime-logs/` (openapi snapshot, prepare/export/preview/confirm JSON evidence), `artifacts/` (8 verified public wheels), `artifacts-local/` (the W3 profile wheel), `public-docs/` (fetched public documentation snapshots). Atrium and API processes stopped at end of session; SurrealDB container `ace12accept-surrealdb-1` stopped, volume preserved for reconciliation.
- 14:10:24Z: all acceptance processes stopped (API, Atrium); containers `ace12accept-*` stopped, volume preserved. Run complete.

## Wall-clock summary

| Step | Window (UTC) | Duration |
|---|---|---|
| S0–S2 J1 locate/install/first-run | 13:42–13:47 | ~5 min |
| S3–S4 J1 incident + isolated launch | 13:46–13:52 | ~6 min |
| S5 J2 Choose (+F3/W3) | 13:53–14:12* | ~12 min (overlaps S6 probes) |
| S6–S8 J3–J5 corpus + blockers | 14:00–14:20* | ~8 min active |
| S9 J7/J8 probes | 14:04–14:06 | ~2 min |
| S10 J9 restart | 14:06–14:07 | ~1 min (32 s downtime) |
| S11 J10 ownership | 14:07–14:08 | ~1 min |
| S12 wind-down + reporting | 14:09–14:10 | ~1 min |

*Log sections were written as steps concluded; probe timestamps in section bodies are authoritative.
