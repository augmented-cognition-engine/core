# Platform P2B — immutable Case closure

**Status:** candidate/local substrate slice verified; its then-open Case-bound Brief gate is now closed by [P2C](platform-p2c-case-bound-governed-brief-v1.md)  
**Date:** 2026-08-07

> **Reproducibility status: candidate/local implementation evidence, not public artifact
> verification.** This record documents work against a local ACE Core 0.4.1 candidate; Core 0.4.1
> itself has no git tag, GitHub Release, or PyPI publication yet, so the versions and wheel digests
> below are local build artifacts, not public releases. The World Intelligence conformance journey
> this record relies on lives in `augmented-cognition-engine/domain-world-intelligence`, whose
> source is public but which carries no version tag, GitHub Release, or PyPI publication. Until both
> repositories are tagged, released, and published, an outside reader cannot independently re-derive
> the identities cited below. This record therefore establishes local implementation evidence only.
> It does not establish the public two-domain neutrality proof (GI2, open per the roadmap), and no
> roadmap outcome may be promoted to `passed` on its basis.

World's 72-hour replay proved that one orientation product can span several independently admitted
developments. Binding those records only inside a prompt would make the aggregation mutable,
unaddressable, and impossible to replay exactly.

## Platform result

- `CaseV1Alpha1` is a domain-neutral, content-addressed Intelligence resource.
- Its identity binds one activation revision, semantic cutoff, assembly time, case type, purpose,
  subject scope, and at least two exact upstream resources.
- Direct members commit transitively to their own immutable lineage; the Case does not copy or
  reinterpret their payloads.
- `PreparedResourceSetAdmissionV1Alpha1` admits and replays the Case through Core's existing
  append-only ledger. No new store, graph, reasoning runtime, attention event, delivery authority,
  or domain policy was introduced.
- Existing single-derivation Brief synthesis remained unchanged. The successor P2C slice now binds
  governed synthesis to one exact Case and its eligible attention receipts without private prompt
  aggregation.

## Consumer result

World freezes five exact developments—four routed Signals and one material Shift that correctly
has no Signal—into:

- Case: `case:2ee200c03f2576307b0bc43e6e128f30`;
- digest: `sha256:2ee200c03f2576307b0bc43e6e128f309e9de7efd3c11b1cad2ad1c250b4591f`;
- complete 28-resource admission: `resource_set_admission:d1833406b2e099cd1cbdfdf38d7ee2a8`;
- admission digest: `sha256:5190fce71b6976bd00da6f8934255b42792d4543197aa7fbf1fed330e0c6722f`.

The World pack supplies only case vocabulary and the pinned conformance composition. World proves
the exact 28-resource admission contract; the ACE fixture proves durable append/restart semantics
through the shared ledger. Case identity, temporal validation, lineage resolution, persistence,
and replay remain shared platform behavior.

## Verification

- ACE Case and independent-admission tests: `4 passed`.
- Complete ACE Intelligence suite: `281 passed`.
- Market conformance suite: `88 passed, 1 skipped`; its pinned historical projections remain
  unchanged by explicitly enumerating the record kinds owned by those earlier packets.
- World replay-specific conformance: `6 passed`.
- Complete World conformance suite: `26 passed, 3 xfailed`.
- Isolated installed-artifact probe: fresh `ace-core==0.3.0` and World `0.3.0` wheels exposed the
  public Case contract and reproduced the pinned Case and 28-resource admission identities outside
  both checkouts. Exact tested-artifact digests are recorded in the World consumer audit so this
  evidence file does not make the wheel hash depend on itself.

At this checkpoint, `WI-CR-005` was narrowed but not closed. The additive P2C successor now closes
it with a Case-bound governed Brief request that derives template/persona eligibility from exact
attention receipts and freezes the Case member context before Core reasoning.
