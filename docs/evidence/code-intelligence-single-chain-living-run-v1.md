# Code Intelligence single-chain living run v1

Date: 2026-08-14 (America/Los_Angeles; execution completed after 00:00 UTC on 2026-08-15)

Disposition: **accepted for the bounded local single-chain claim**

This packet closes one continuous local chain: exact initial Python lens and symbol-aware handoff,
provider-neutral return, independently observed check, exact one-path incremental update,
generation-two append, provider/rescan-forbidden fresh-process reopen, post-restart lens/source
identity, and immutable generation-one readability. The machine-readable receipt is
[code-intelligence-single-chain-living-run-v1.json](code-intelligence-single-chain-living-run-v1.json).

It is additive evidence after installed-wheel v4. It does not reinterpret or modify v1-v4, and it
is not installed-wheel, release, publication, deployment, production grant, delivery, effect,
backup, rollback, recovery, or #194-closeout evidence.

## Exact chain

The disposable repository contained three Python files, three functions, and two imports. The
initial bounded block explicitly named `transform`, carried context lines 1-6, and identified the
implementation span as lines 4-6. Its exact block was
`code_context_block:3bb91bae21a29483b81bc4184cfc325f`; the handoff was
`coding_agent_handoff:83b3c783bf18b5ad7c05f845c7ab6f3c`. Deserialization independently parses
the exact symbol-body slice, verifies that it defines `transform`, and binds that slice to the
journey source anchor; coherent metadata and ID recomputation cannot make a `pass` body qualify.

The exact provider-neutral return named only `pkg/service.py` and closed all three context blocks.
The bounded return receipt, independent subprocess observation, and living update identities were:

- return `coding_agent_return:020c2ad41b08415e8b65ea5d9956e521`;
- receipt `coding_agent_return_receipt:8aced5f8054b77918b7fd776eceb6d94`;
- verification `code_verification:bfc4f454e1c155a6b7fe59792109b509`;
- mutation `code_file_mutation:4e6dc3b66f06ef34afbbddd992b48a00`; and
- living update `code_living_update:2d4dfe7e4a953311e1b63c707315551a`.

The independent observer executed the changed behavior itself and observed exit code zero. It did
not accept the coding agent's verification reference as proof. The harness separately read Git's
changed-path inventory and required it to equal the return's sole `pkg/service.py` path before
verification, incremental indexing, or chain closure.

The harness-applied local mutation preserves the exact 136-byte before and after source bodies.
Their SHA-256 values are respectively
`c0eca19cf8cac8d0b15e026f54978a170b0e147a0202ca2a7f88f1002ff16ab2` and
`47f80352745bf6ae10dab910c91e611d02e3bbb47ee20b4cd23b186b7ec9b9de`. The deterministic
175-byte unified patch has SHA-256
`c00200ef4b5a4808815d044795d3a496403a9f32f1142ec686f034bf8c65b38f`. These bodies and patch
are embedded in the verification contract and crosslinked by the living update. They record a
harness mutation, not coding-agent delivery, external delivery, or production effect.

Generation two `code_index_snapshot:8cf9d89618434d14f45db5b17c60541a` names generation one
`code_index_snapshot:bd9a2b487c2ab8dbe14d20a8a15b9603` and its exact digest as parent. A
fresh Python process reopened generation two with full/incremental scans and provider import or
invocation forbidden. It regenerated lens `atrium_code_lens:5d6d60f80a72672081376d02ef7ec704`
and observed the exact post-change `transform` span at lines 4-6, source-file SHA-256
`47f80352745bf6ae10dab910c91e611d02e3bbb47ee20b4cd23b186b7ec9b9de`. Generation one remained
readable with an unchanged digest.

The complete run is `code_living_run:070d336c7419eb435b9963e1c196d4b5`. Its raw 63,092-byte packet,
SHA-256 `5c3dab839c0f207345760e9ee2be855adc94d890d97d698078f237457a66a28d`,
is preserved as the sole member of the 9,029-byte
[durable replay archive](artifacts/code-intelligence-single-chain-living-run-replay-v1.tar.gz),
SHA-256 `696548f0ea3322cddf1a66914499a12a87a09921c3cac1ee62119788a4ffe7a5`.
The raw member includes the complete generation-one, generation-two, and historical generation-one
snapshot contracts plus the complete post-restart journey and source block. Paired replay requires
the separately recorded raw-member, run, receipt/time, both snapshot, and post-restart lens
coordinates before validating their internal linkage.

The additive receipt also binds the unchanged predecessor files directly:

| Evidence | Bytes | SHA-256 |
|---|---:|---|
| installed wheel v1 | 5,284 | `284005cde84c9d232b4307b2574eda3e9890f042ce387a2a586b50e5ec153b53` |
| installed wheel v2 | 8,456 | `4bdb7bcb3ccea59e130d9fd77e1a0ea2e62978010ec7ed2c7a1a7323d522691e` |
| installed wheel v3 | 18,369 | `4b3cb2d4a772eb4682b19fe94c2f9011a84ec77dbc3359f8538a762fe1aec2ef` |
| installed wheel v4 | 14,183 | `d9ba419189cd4f3602939beb9e8047d8b778ef131a77888f3c115483cc56d756` |

## Adversarial and regression gates

The 30-test focused gate passed journey, named-symbol snippet, return, snapshot, prior continuity,
and living-run cases. A widened 205-test Code Intelligence, incremental, graph, MCP, and public-Core
boundary gate also passed. Ruff passed.

Focused adversarial cases reject:

- changed paths that differ across return, receipt, verification, and living update;
- a generation-two parent ID or digest that differs from generation one;
- a forged verification status/exit-code pair;
- any authority-positive or extra field;
- a run bound to a different initial journey; and
- named-symbol identity/span fields that are partial, reversed, outside the bounded body, or do not
  parse to the named Python definition;
- coherent receipt-time and snapshot forgeries after dependent IDs are recomputed; and
- a patch that differs from the deterministic before/after diff even with coherent digest fields.

## Authority and limitations

Source, reasoning, change, approval, delivery, execution, and effect authority are all false. The
fresh-process path is provider-neutral; it neither imports nor invokes a coding provider.

A deserialized verification observation and its output digests are structurally bound but do not
self-authenticate command execution. The checked-in replay proves contract/identity closure and
requires verifier replay; it does not turn stored digests into execution authority or historical
attestation. Semantic return-receipt reconstruction excludes `validated_at`, because replay is a new
observation, but compares every semantic and authority field. Paired archive validation separately
requires both the exact externally recorded receipt ID and `validated_at`, preventing coherent
timestamp/ID recomputation from replacing the recorded event.

This proves one local Python/single-repository path only. The independently observed subprocess
proves only its fixture assertion. Exact changed paths remain caller-supplied; there is no watcher,
LSP, compiler, additional language profile, multi-repository topology, deployment, external
delivery, production effect, backup, rollback, or recovery UX in this packet.
