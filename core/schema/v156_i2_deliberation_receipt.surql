-- I2: additive bounded attributable-deliberation projection.
-- The field stores only observable selection signals, bounded contributor
-- final artifacts, conflict lineage, synthesis dispositions, and degraded
-- coverage. It adds no table, write authority, transcript, or MCP operation.

DEFINE FIELD IF NOT EXISTS deliberation_receipt ON TABLE task TYPE option<object>;
