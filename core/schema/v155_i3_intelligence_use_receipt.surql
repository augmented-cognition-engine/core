-- I3: additive, bounded retained-intelligence continuity projection.
-- The canonical identities remain task, decision, observation, and insight.
-- This field stores only the versioned read receipt; it grants no write or
-- execution authority and does not add a public MCP operation.

DEFINE FIELD IF NOT EXISTS intelligence_use_receipt ON TABLE task TYPE option<object>;
